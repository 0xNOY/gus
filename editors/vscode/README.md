# GUS for Visual Studio Code

GUS displays profile selection prompts and protection status inside VS Code.
It runs as a workspace extension, so Remote SSH uses the GUS installation and
broker on the remote Git host; no GUS binary is required on the client PC.

This extension is currently a preview and is installed as part of a matching
GUS release. The extension and native broker protocol versions must match.
